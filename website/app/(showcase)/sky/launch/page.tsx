import { LaunchSkyDirector } from '../../../../modules/launch-video/launch-sky-director'

export const metadata = {
  title: 'Solaris — Sky launch capture',
  robots: { index: false, follow: false },
}

export default function SkyLaunchPage() {
  return <LaunchSkyDirector />
}
